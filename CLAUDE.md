# Reels Generator — Project Rules

## Instagram ID (필수 삽입)

**`@gi_seong_5253`** — 앞으로 생성하는 모든 릴스 디자인의 **하단 CTA 포스트잇 최상단**에 반드시 빨간 펜 톤(`RED_INK`)으로 삽입.

레이아웃: `@gi_seong_5253` (id) → `댓글에 "정보" 남기면` (메인 카피) → `DM으로 …` (서브)

## 필수 유의문구 (모든 릴스 영상)

`reels/scripts/reels_overlay.py` 의 `draw_bottom_disclaimer()` 와 `draw_final_disclosure()` 를 모든 새 스크립트의 `render_frame()` 끝에 호출.

### A. 하단 지속 노출 유의문구 (0~13초)
- **글자 크기**: 영상 세로의 1/40 이상 = **48pt 이상** (강제)
- **노출 위치**: 화면 하단 띠 (y≈1810~1920)
- **3개 문구 회전** (각 ~5초):
  1. `본 내용은 모집종사자 개인의 의견이며, 계약체결에 따른 이익 또는 손실은 보험계약자 및 피보험자에게 귀속됩니다`
  2. `보험사 및 상품별로 상이할 수 있으므로, 관련한 세부사항은 반드시 해당 약관을 참조바랍니다`
  3. `보험회사 상품별, 성별·연령·직업 등에 따라 가입가능한 담보와 가입금액·보험료 등은 달라질 수 있습니다`

### B. 마지막 풀스크린 필수고지 (13~15초, 2초 이상)
- **설계사**: `프라임에셋 박기성 (손·생보 협회 등록번호 202511200002261)`
- **심의필**: `제[----]호 ([심의일]~[유효일])` — 심의 받은 후 placeholder 교체
- **광고심의 문구**: `본 광고는 광고심의기준을 준수하였으며, 유효기간은 심의일로부터 1년입니다`
- **승환계약 안내**:
  - ① 질병이력·연령증가 등으로 가입이 거절되거나 보험료가 인상될 수 있습니다
  - ② 가입 상품에 따라 새로운 면책기간 적용 및 보장 제한 등 기타 불이익이 발생할 수 있습니다

### C. CTA 위치 조정
하단 유의문구 띠와 겹치지 않도록 모든 CTA의 bottom-align Y를 `H` (=1920) 가 아닌 **`1810`** 으로 사용.

## 디자인 시스템 / 광고규제 / CTA 규칙

릴스 생성 시 시스템 프롬프트의 디자인 시스템(컬러·폰트·손그림 요소), 애니메이션 타임라인, 보험 광고규제 회피, CTA 통일 규칙을 모두 준수.

## 작업 위치

- 작업 폴더: `reels/`
- 공통 오버레이 모듈: `reels/scripts/reels_overlay.py`
- 스크립트: `reels/scripts/reels_NN_<slug>.py`
- 프레임: `reels/frames/` (gitignore)
- 영상: `reels/output/reels_NN.mp4` (gitignore)
- 정적 프리뷰: `reels/output/static_NN_preview.png` (커밋 가능)
- 폰트: `reels/fonts/`
